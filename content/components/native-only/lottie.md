---
title: lottie
description: No built-in Lottie player ships with the runtime; `<lottie>` is a MODULE element, and the Core/Lottie web facet (the vendored lottie-web light player, MIT, pinned) fills the tag through the module component loader whenever the module is registered.
order: 3
section: components
element: lottie
category: media
scope: module-owned
platforms: ios,android
properties: [{"name":"autoplay","type":"bool","default":"true"},{"name":"loop","type":"bool","default":"true"},{"name":"speed","type":"number","default":"1"},{"name":"src","type":"url","default":null}]
actions: []
catalog: 0.1.0
commit: 4a6e08719f45b7be669db8dcc9133241de5191ac
generator: ClosedSource/scripts/generate_component_docs.rb
---

# lottie

No built-in Lottie player ships with the runtime; `<lottie>` is a MODULE element, and the Core/Lottie web facet (the vendored lottie-web light player, MIT, pinned) fills the tag through the module component loader whenever the module is registered.

<RefMeta platforms="iOS,Android">
Category: Media - Live specimens: the [System gallery](/system).
</RefMeta>

This element is web unsupported by DESIGN, not by omission: its essence is a vendor or platform runtime the web renderer does not ship, and its facet fills the tag only when its module is registered. It is named here rather than left off the list, because a library that quietly omits its own rows is not a library.

**The ruling**, 2026-08-18. component-library.md 'Out of scope, named': native-first module tags follow their module roadmaps; the row stays on the scoreboard without blocking the trinity.

Module: `lottie`.

## Usage

```dsx
<lottie src="confetti.json" loop="true"/>
```

`lottie` takes no children.

## Attributes

| Attribute | Type | Default | Notes |
|---|---|---|---|
| `autoplay` | `bool` | `true` |  |
| `loop` | `bool` | `true` |  |
| `speed` | `number` | `1` |  |
| `src` | `url` |  |  |

Every element also carries the [universal attributes](/components/attributes): accessibility, animation, `class`, `style` and the platform suffixes.

## Events

`lottie` raises no events.

## Platform notes

| Renderer | Grammar | What that means |
|---|---|---|
| web | `unsupported` | no renderer and no polyfill; the runtime mounts the labelled placeholder |
| ios | `reference` | the reference renderer this element is specified against |
| android | `module-facet` | filled by the module facet when that module is registered |

No built-in Lottie player ships with the runtime; `<lottie>` is a MODULE element, and the Core/Lottie web facet (the vendored lottie-web light player, MIT, pinned) fills the tag through the module component loader whenever the module is registered.

**Known limits on the web**

- With Core/Lottie registered the facet consumes src (a Lottie .json URL, absolute or site-relative), loop, speed, autoplay, a11yLabel and on:finish; loading and failure render labelled states inside the element's own box, and prefers-reduced-motion pins the static first frame.
- The module facet renders via SVG only and plays plain Lottie JSON: dotLottie (.lottie) archives, bundled-name lookup, and expression-driven animations are not supported on this surface.
- Without the module (excluded builds, sliced embeds), src, loop, speed, and autoplay are not consumed.

**Fallback.** Mounts the honest dsx-unsupported placeholder whenever the Core/Lottie module is absent - a labelled marker carrying the `<lottie>` name and the dsx-unsupported class; never blank, never a fake lookalike of the native surface.

Declared platforms: `ios`, `android`.

This page is GENERATED by ClosedSource/scripts/generate_component_docs.rb. A hand edit here is overwritten on the next run by design: fix the ledger instead (the attribute and event contract in `OpenSource/Documentation/reference/stack-elements.json`, the platform support and the audit in `OpenSource/Conformance/library/matrix.json`, the description and the web limits in `OpenSource/Web/support/element-support.json`, the specimen in `OpenSource/Catalog`).

