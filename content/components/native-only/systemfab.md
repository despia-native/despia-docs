---
title: SystemFAB
description: The native half of the shared FAB.dsx wrapper has no Web renderer, and is not asked for one: FAB.dsx gates this tag off web, Windows and Linux and gives those lanes a pressable composition instead, so the floating action button an author writes as `<FAB>` is a real, styled, accessible control on every renderer while this tag itself stays native.
order: 3
section: components
element: SystemFAB
category: input
scope: module-owned
platforms: ios,android
properties: [{"name":"a11yLabel","type":"string","default":null},{"name":"icon","type":"sf-symbol","default":"plus"},{"name":"on:tap","type":"action","default":null}]
actions: ["tap"]
catalog: 0.1.0
commit: a0939217acc2dea008185a0feb4874bd2765c7a0
generator: ClosedSource/scripts/generate_component_docs.rb
---

# SystemFAB

The native half of the shared FAB.dsx wrapper has no Web renderer, and is not asked for one: FAB.dsx gates this tag off web, Windows and Linux and gives those lanes a pressable composition instead, so the floating action button an author writes as `<FAB>` is a real, styled, accessible control on every renderer while this tag itself stays native.

<RefMeta platforms="iOS,Android">
Category: Input - Live specimens: the [System gallery](/system).
</RefMeta>

This element is web unsupported by DESIGN, not by omission: its essence is a vendor or platform runtime the web renderer does not ship, and its facet fills the tag only when its module is registered. It is named here rather than left off the list, because a library that quietly omits its own rows is not a library.

**The ruling**, 2026-08-18. component-library.md 'Out of scope, named': native-first module tags follow their module roadmaps; the row stays on the scoreboard without blocking the trinity.

## Usage

```dsx
<SystemFAB icon="plus" a11yLabel="New" on:tap="dsx.send('new')"/>
```

`SystemFAB` takes no children.

## Attributes

| Attribute | Type | Default | Notes |
|---|---|---|---|
| `a11yLabel` | `string` |  |  |
| `icon` | `sf-symbol` | `plus` |  |
| `on:tap` | `action` |  |  |

Every element also carries the [universal attributes](/components/attributes): accessibility, animation, `class`, `style` and the platform suffixes.

## Events

| Event | Handler |
|---|---|
| `tap` | `on:tap="..."` |

A payload arrives FLAT in the handler scope, so a declared action names the key bare (`message="message"`), never through an `event` plane.

## Platform notes

| Renderer | Grammar | What that means |
|---|---|---|
| web | `unsupported` | no renderer and no polyfill; the runtime mounts the labelled placeholder |
| ios | `reference` | the reference renderer this element is specified against |
| android | `enforced` | implemented and pinned by the element parity test |

The native half of the shared FAB.dsx wrapper has no Web renderer, and is not asked for one: FAB.dsx gates this tag off web, Windows and Linux and gives those lanes a pressable composition instead, so the floating action button an author writes as `<FAB>` is a real, styled, accessible control on every renderer while this tag itself stays native.

**Known limits on the web**

- No DOM lookalike of the SwiftUI Button or the Material 3 FloatingActionButton is mounted, so nothing here is reported as a working floating action button.
- The census row records what already ships on the two native lanes; it grants the Web renderer nothing.

**Fallback.** An author who writes `<FAB>` gets the wrapper's own web branch, a pressable carrying the dsx-fab class with the same icon, accessibility label and tap event. A document that writes the native tag directly mounts the honest dsx-unsupported placeholder, a labelled marker carrying the `<SystemFAB>` name; never blank, never a fake lookalike.

**Implementation notes.** The native half of the shared FAB.dsx wrapper, and the two lanes agree on the whole contract because both read the same three words and the same two numbers. What differs is the platform identity underneath: SwiftUI owns the Button interaction, focus and activation on Apple, while Material 3 owns the visual, ripple, elevation and focus on Android, where the composable also collapses the FAB into one truthful accessibility node so the export is not a nameless clickable plus a raw glyph. The size and icon size are read from the resolved width, height and font size rather than hardcoded, which is why they are recorded as the builder's fallbacks.

Declared platforms: `ios`, `android`.

This page is GENERATED by ClosedSource/scripts/generate_component_docs.rb. A hand edit here is overwritten on the next run by design: fix the ledger instead (the attribute and event contract in `OpenSource/Documentation/reference/stack-elements.json`, the platform support and the audit in `OpenSource/Conformance/library/matrix.json`, the description and the web limits in `OpenSource/Web/support/element-support.json`, the specimen in `OpenSource/Catalog`).

