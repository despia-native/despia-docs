---
title: Native first elements
description: The rows whose essence is a platform or vendor runtime the web renderer does not ship.
order: 3
section: components
element: 
category: 
scope: reference
platforms: ios,android
properties: []
actions: []
catalog: 0.1.0
commit: 4fee8f0f180a24140dc54c148df88454bef5e365
generator: ClosedSource/scripts/generate_component_docs.rb
---

# Native first elements

These 14 elements are web unsupported by DESIGN and follow their own module roadmaps. They are on the scoreboard, with a dated ruling each, rather than left off the list.

<CardGroup cols="2">
<Card title="LevelMeter" href="/components/native-only/levelmeter">
The Studio audio level meter has no Web renderer or audio-analysis seam.
</Card>
<Card title="MenuSidebar" href="/components/native-only/menusidebar">
The native half of the shared Sidebar.dsx adapter has no Web renderer: the slide-over panel, its scrim and its rows are composed by SwiftUI on Apple and by Material 3 ModalNavigationDrawer on Android, and a web build authors its own navigation in markup instead.
</Card>
<Card title="Scene360" href="/components/native-only/scene360">
No panorama renderer is registered for Web.
</Card>
<Card title="Scene3D" href="/components/native-only/scene3d">
The RealityKit scene contract has no WebGL/WebGPU model renderer.
</Card>
<Card title="StudioPitchEditor" href="/components/native-only/studiopitcheditor">
The Studio pitch-editing surface has no Web DSP/editor implementation.
</Card>
<Card title="StudioShow" href="/components/native-only/studioshow">
The Studio mode-switching surface has no Web implementation.
</Card>
<Card title="StudioTimecode" href="/components/native-only/studiotimecode">
The Studio synchronized timecode display has no Web transport-clock integration.
</Card>
<Card title="StudioTimeline" href="/components/native-only/studiotimeline">
The multi-track Studio timeline/editor has no Web implementation.
</Card>
<Card title="StudioTrim" href="/components/native-only/studiotrim">
The Studio clip-trimming surface has no Web implementation.
</Card>
<Card title="SystemFAB" href="/components/native-only/systemfab">
The native half of the shared FAB.dsx wrapper has no Web renderer, and is not asked for one: FAB.dsx gates this tag off web, Windows and Linux and gives those lanes a pressable composition instead, so the floating action button an author writes as `<FAB>` is a real, styled, accessible control on every renderer while this tag itself stays native.
</Card>
<Card title="SystemSettingsRow" href="/components/native-only/systemsettingsrow">
The native half of the shared SettingsRow.dsx wrapper has no Web renderer, and is not asked for one: SettingsRow.dsx gates this tag off web, Windows and Linux and composes the row from standard elements and CSS on those lanes, so the settings row an author writes as `<SettingsRow>` is a real, styled, accessible row on every renderer while this tag itself stays native.
</Card>
<Card title="Waveform" href="/components/native-only/waveform">
The Studio waveform surface has no Web renderer.
</Card>
<Card title="lottie" href="/components/native-only/lottie">
No built-in Lottie player ships with the runtime; `<lottie>` is a MODULE element, and the Core/Lottie web facet (the vendored lottie-web light player, MIT, pinned) fills the tag through the module component loader whenever the module is registered.
</Card>
<Card title="rive" href="/components/native-only/rive">
No Rive runtime ships with the web renderer; `<rive>` is a MODULE element, and the Core/Rive web facet fills the tag through the module component loader whenever the module is registered and its pinned @rive-app/canvas player was fetched by the build.
</Card>
</CardGroup>

This page is GENERATED by ClosedSource/scripts/generate_component_docs.rb. A hand edit here is overwritten on the next run by design: fix the ledger instead (the attribute and event contract in `OpenSource/Documentation/reference/stack-elements.json`, the platform support and the audit in `OpenSource/Conformance/library/matrix.json`, the description and the web limits in `OpenSource/Web/support/element-support.json`, the specimen in `OpenSource/Catalog`).

